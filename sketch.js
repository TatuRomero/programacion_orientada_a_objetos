let posX, posY;
let diametro;
let radio;

function setup() {
  createCanvas(windowWidth, windowHeight);

  radio = ceil(random(5, 25));
  diametro = radio * 2;
  // console.log(diametro);
}

function draw() {
  background(220);
}
